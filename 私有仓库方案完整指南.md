# MkSaaS 私有仓库方案完整指南

## 📋 方案概述

将5个子项目配置为使用你的私有仓库，同时保留官方仓库用于同步更新。

### 架构图

```
你的私有仓库 (origin)          官方仓库 (upstream)
ameureka/mksaas-app    ←→    MkSaaSHQ/mksaas-app
     ↑                              ↓
     └──── 你的修改推送这里    从这里拉取官方更新
```

---

## 🚀 实施步骤

### 第一步：创建私有仓库（手动操作）⏱️ 5分钟

1. 访问 GitHub 创建页面：https://github.com/new

2. 创建5个私有仓库（每个重复以下步骤）：

   **仓库1：mksaas-app**
   ```
   Repository name: mksaas-app
   Description: MkSaaS AI 工具站 - 私有仓库
   ⚫ Private ← 必须选择
   ☐ 不要勾选任何选项
   ```
   点击 "Create repository"

   **仓库2：mksaas-blog**
   ```
   Repository name: mksaas-blog
   Description: MkSaaS 博客平台 - 私有仓库
   ⚫ Private
   ```

   **仓库3：mksaas-haitang**
   ```
   Repository name: mksaas-haitang
   Description: MkSaaS 导航站 - 私有仓库
   ⚫ Private
   ```

   **仓库4：mksaas-outfit**
   ```
   Repository name: mksaas-outfit
   Description: MkSaaS AI 试衣站 - 私有仓库
   ⚫ Private
   ```

   **仓库5：mksaas-template**
   ```
   Repository name: mksaas-template
   Description: MkSaaS 基础模板 - 私有仓库
   ⚫ Private
   ```

3. 验证：访问 https://github.com/ameureka?tab=repositories
   
   应该看到5个带 🔒 标签的私有仓库

---

### 第二步：运行配置脚本 ⏱️ 2分钟

在主仓库根目录运行：

```bash
./配置私有仓库脚本.sh
```

**脚本会自动**：
1. ✅ 保存官方仓库 URL 为 upstream
2. ✅ 更新 origin 为你的私有仓库
3. ✅ 推送代码到你的私有仓库
4. ✅ 验证配置

**预期输出**：
```
========================================
📦 配置项目: mksaas-app
========================================
1️⃣ 获取当前 remote 信息...
   当前 origin: https://github.com/MkSaaSHQ/mksaas-app.git
3️⃣ 添加官方仓库为 upstream...
✅ upstream 添加成功
4️⃣ 更新 origin 为你的私有仓库...
✅ origin 更新成功
5️⃣ 验证 remote 配置...
   Remote 列表：
   origin    https://github.com/ameureka/mksaas-app.git (fetch)
   origin    https://github.com/ameureka/mksaas-app.git (push)
   upstream  https://github.com/MkSaaSHQ/mksaas-app.git (fetch)
   upstream  https://github.com/MkSaaSHQ/mksaas-app.git (push)
6️⃣ 推送到你的私有仓库...
✅ 推送成功
```

---

### 第三步：验证配置 ⏱️ 1分钟

```bash
# 检查每个项目的 remote 配置
for project in mksaas-app mksaas-blog mksaas-haitang mksaas-outfit mksaas-template; do
    echo "=== $project ==="
    git -C $project remote -v
    echo ""
done
```

**应该看到**：
```
=== mksaas-app ===
origin    https://github.com/ameureka/mksaas-app.git (你的私有仓库)
upstream  https://github.com/MkSaaSHQ/mksaas-app.git (官方仓库)
```

---

## 📚 日常使用

### 场景1：修改代码并推送

```bash
# 1. 进入子项目
cd mksaas-app

# 2. 创建功能分支（推荐）
git checkout -b feature/my-custom-feature

# 3. 修改代码
# ... 编辑文件 ...

# 4. 提交
git add .
git commit -m "feat: 添加自定义功能"

# 5. 推送到你的私有仓库
git push origin feature/my-custom-feature

# 6. 回到主仓库
cd ..
```

---

### 场景2：同步官方更新 ⭐

#### 方法A：使用脚本（推荐）

```bash
# 在主仓库根目录运行
./同步官方更新脚本.sh
```

脚本会：
1. ✅ 从官方仓库拉取最新更新
2. ✅ 合并到你的代码
3. ✅ 推送到你的私有仓库
4. ✅ 自动处理所有5个项目

#### 方法B：手动同步（单个项目）

```bash
# 1. 进入子项目
cd mksaas-app

# 2. 确保在主分支
git checkout main

# 3. 拉取官方更新
git fetch upstream

# 4. 合并官方更新
git merge upstream/main
# 或者使用 rebase（保持历史整洁）
# git rebase upstream/main

# 5. 推送到你的私有仓库
git push origin main

# 6. 回到主仓库
cd ..
```

---

### 场景3：合并官方更新到你的功能分支

```bash
# 1. 进入子项目
cd mksaas-app

# 2. 先更新主分支
git checkout main
git fetch upstream
git merge upstream/main
git push origin main

# 3. 切换到你的功能分支
git checkout feature/my-custom-feature

# 4. 合并主分支的更新
git merge main
# 或者 rebase
# git rebase main

# 5. 解决冲突（如果有）
# ... 编辑冲突文件 ...
git add .
git commit -m "merge: 合并官方更新"

# 6. 推送
git push origin feature/my-custom-feature

# 7. 回到主仓库
cd ..
```

---

### 场景4：更新主仓库的子项目引用

如果你在主仓库中将子项目作为 Git Submodule：

```bash
# 在主仓库根目录

# 1. 更新子项目引用
git add mksaas-app
git commit -m "update: 更新 mksaas-app 到最新版本"

# 2. 推送
git push origin main
```

---

## 🔧 常用命令

### 查看 remote 配置

```bash
cd mksaas-app
git remote -v
```

### 查看当前分支

```bash
cd mksaas-app
git branch
```

### 查看提交历史

```bash
cd mksaas-app
git log --oneline --graph --all
```

### 查看未提交的更改

```bash
cd mksaas-app
git status
```

### 暂存当前更改

```bash
cd mksaas-app
git stash
# 恢复
git stash pop
```

---

## ⚠️ 常见问题

### Q1: 推送失败，提示 "rejected"

**原因**：远程仓库有你本地没有的提交

**解决**：
```bash
# 先拉取
git pull origin main

# 再推送
git push origin main
```

### Q2: 合并时出现冲突

**解决**：
```bash
# 1. 查看冲突文件
git status

# 2. 编辑冲突文件，解决冲突标记
# <<<<<<< HEAD
# 你的代码
# =======
# 官方代码
# >>>>>>> upstream/main

# 3. 标记为已解决
git add <冲突文件>

# 4. 完成合并
git commit -m "merge: 解决冲突"
```

### Q3: 想要放弃本地修改

**解决**：
```bash
# 放弃所有未提交的修改
git reset --hard HEAD

# 或者只放弃某个文件
git checkout -- <文件名>
```

### Q4: 想要撤销最后一次提交

**解决**：
```bash
# 撤销提交但保留更改
git reset --soft HEAD~1

# 撤销提交并丢弃更改
git reset --hard HEAD~1
```

---

## 📊 配置验证清单

完成配置后，检查以下项目：

- [ ] 5个私有仓库已在 GitHub 创建
- [ ] 每个仓库都有 🔒 Private 标签
- [ ] 每个子项目的 origin 指向你的私有仓库
- [ ] 每个子项目的 upstream 指向官方仓库
- [ ] 代码已成功推送到私有仓库
- [ ] 可以正常拉取官方更新

---

## 🎯 优势总结

### ✅ 完全私有
- 你的代码和配置完全私有
- 保护 API 密钥和敏感信息

### ✅ 可以修改
- 自由添加自定义功能
- 修改任何代码

### ✅ 可以同步官方
- 随时拉取官方更新
- 获取新功能和修复

### ✅ 灵活管理
- 使用分支管理功能
- 独立版本控制

---

## 📞 获取帮助

如果遇到问题：

1. 查看脚本输出的错误信息
2. 检查 GitHub 仓库是否正确创建
3. 验证网络连接
4. 检查 Git 配置

---

**准备好了吗？开始第一步：创建私有仓库！**
