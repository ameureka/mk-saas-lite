# 进入任意项目目录
cd mksaas-blog

# 拉取最新代码
git pull origin main

for repo in mksaas-template mksaas-blog mksaas-haitang mksaas-app mksaas-outfit; do
  echo "更新 $repo..."
  cd $repo && git pull && cd ..
done
