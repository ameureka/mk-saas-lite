# 问题13：涉及知识形式的模板如何购买，如何使用以及设计模板，涉及模板插件与目前的 MkSaaS 之间关系是什么？

## 概述

MkSaaS 是一个付费的 SaaS 模板系统，提供完整的代码库和 5 个不同用途的仓库模板。购买后获得 GitHub 私有仓库访问权限，可以自由使用、修改和部署。

## 一、模板购买

### 1. 购买方式

#### 官方网站购买

- **网站**: [mksaas.com](https://mksaas.com)
- **价格**: $199 USD（终身访问）
- **支付方式**: Stripe（信用卡、Apple Pay、Google Pay）

#### 购买流程

1. 访问 [mksaas.com](https://mksaas.com)
2. 点击 "Get Started" 或 "Buy Now"
3. 填写 GitHub 用户名
4. 完成 Stripe 支付
5. 自动获得 GitHub 仓库访问权限
6. 收到确认邮件

### 2. 购买后获得什么

#### 5 个 GitHub 私有仓库访问权限

```
1. mksaas-template (基础模板)
   - URL: https://github.com/MkSaaSHQ/mksaas-template
   - 演示: https://demo.mksaas.com
   - 状态: ✅ Ready
   - 用途: 通用 SaaS 应用模板

2. mksaas-blog (博客模板)
   - URL: https://github.com/MkSaaSHQ/mksaas-blog
   - 演示: https://mksaas.me
   - 状态: ✅ Ready
   - 用途: 内容驱动的博客网站

3. mksaas-haitang (海棠诗社)
   - URL: https://github.com/MkSaaSHQ/mksaas-haitang
   - 演示: https://haitang.app
   - 状态: ✅ Ready
   - 用途: 特定领域应用示例

4. mksaas-outfit (服装应用)
   - URL: https://github.com/MkSaaSHQ/mksaas-outfit
   - 演示: 暂无
   - 状态: ✅ Ready
   - 用途: 电商/展示类应用

5. mksaas-app (应用模板)
   - URL: https://github.com/MkSaaSHQ/mksaas-app
   - 演示: https://mksaas.app
   - 状态: 🚧 WIP (Work In Progress)
   - 用途: 纯应用型 SaaS
```

#### 额外资源

- **完整文档**: [mksaas.com/docs](https://mksaas.com/docs)
- **Discord 社区**: [mksaas.link/discord](https://mksaas.link/discord)
- **视频教程**: [mksaas.link/youtube](https://mksaas.link/youtube)
- **更新日志**: [mksaas roadmap](https://mksaas.link/roadmap)
- **技术支持**: support@mksaas.com

### 3. 许可证

#### 使用权限

```
✅ 允许:
- 用于个人项目
- 用于商业项目
- 修改代码
- 创建衍生作品
- 部署到生产环境
- 创建多个项目

❌ 禁止:
- 转售模板本身
- 分享给未购买的用户
- 公开源代码
- 创建竞品模板
```

详细许可证信息请查看仓库中的 `LICENSE` 文件。

## 二、模板使用

### 1. 获取代码

#### 克隆仓库

```bash
# 克隆你需要的模板
git clone https://github.com/MkSaaSHQ/mksaas-template my-project
cd my-project

# 或克隆所有模板
git clone https://github.com/MkSaaSHQ/mksaas-template
git clone https://github.com/MkSaaSHQ/mksaas-blog
git clone https://github.com/MkSaaSHQ/mksaas-haitang
git clone https://github.com/MkSaaSHQ/mksaas-outfit
git clone https://github.com/MkSaaSHQ/mksaas-app
```

#### 创建自己的仓库

```bash
# 方法1: 使用 GitHub 模板功能
# 在 GitHub 网页上点击 "Use this template" 按钮

# 方法2: 手动创建
cd my-project
rm -rf .git
git init
git add .
git commit -m "Initial commit from MkSaaS template"
git remote add origin https://github.com/yourusername/your-repo.git
git push -u origin main
```

### 2. 初始化项目

#### 安装依赖

```bash
# 使用 pnpm（推荐）
pnpm install

# 或使用 npm
npm install

# 或使用 yarn
yarn install
```

#### 配置环境变量

```bash
# 复制环境变量模板
cp env.example .env.local

# 编辑 .env.local，填入你的配置
```

必需的环境变量：

```env
# 数据库
DATABASE_URL=postgresql://...

# 认证
BETTER_AUTH_SECRET=your-secret-key
BETTER_AUTH_URL=http://localhost:3000

# OAuth（可选）
GITHUB_CLIENT_ID=...
GITHUB_CLIENT_SECRET=...
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...

# 支付
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# 邮件
RESEND_API_KEY=re_...

# 存储（可选）
STORAGE_ACCESS_KEY_ID=...
STORAGE_SECRET_ACCESS_KEY=...
STORAGE_BUCKET_NAME=...
```

#### 初始化数据库

```bash
# 推送 schema 到数据库
pnpm db:push

# 或运行迁移
pnpm db:generate
pnpm db:migrate
```

#### 启动开发服务器

```bash
pnpm dev
```

访问 `http://localhost:3000`（或配置的端口）

### 3. 自定义配置

#### 网站基本信息

编辑 `src/config/website.tsx`:

```typescript
export const websiteConfig: WebsiteConfig = {
  ui: {
    theme: {
      defaultTheme: 'default',  // 默认主题
      enableSwitch: true,        // 启用主题切换
    },
    mode: {
      defaultMode: 'dark',       // 默认模式
      enableSwitch: true,        // 启用模式切换
    },
  },
  metadata: {
    images: {
      ogImage: '/og.png',        // Open Graph 图片
      logoLight: '/logo.png',    // 亮色 Logo
      logoDark: '/logo-dark.png', // 暗色 Logo
    },
    social: {
      github: 'https://github.com/yourusername',
      twitter: 'https://twitter.com/yourusername',
      discord: 'https://discord.gg/your-server',
      // ... 其他社交媒体
    },
  },
  features: {
    enableUpgradeCard: true,     // 启用升级卡片
    enableUpdateAvatar: true,    // 启用头像更新
    enableCrispChat: false,      // 启用 Crisp 聊天
    enableTurnstileCaptcha: false, // 启用 Turnstile 验证码
  },
  auth: {
    enableGoogleLogin: true,     // 启用 Google 登录
    enableGithubLogin: true,     // 启用 GitHub 登录
    enableCredentialLogin: true, // 启用邮箱密码登录
  },
  i18n: {
    defaultLocale: 'en',         // 默认语言
    locales: {
      en: {
        flag: '🇺🇸',
        name: 'English',
        hreflang: 'en',
      },
      zh: {
        flag: '🇨🇳',
        name: '中文',
        hreflang: 'zh-CN',
      },
    },
  },
  blog: {
    enable: true,                // 启用博客
    paginationSize: 12,          // 每页文章数
    relatedPostsSize: 3,         // 相关文章数
  },
  payment: {
    provider: 'stripe',          // 支付提供商
  },
  // ... 更多配置
};
```

#### 价格计划配置

编辑 `src/config/price-config.tsx`:

```typescript
export const priceConfig = {
  plans: {
    free: {
      id: 'free',
      name: 'Free',
      description: 'For individuals',
      features: [
        '基础功能',
        '50 积分/月',
        '社区支持',
      ],
      prices: [],
      isFree: true,
    },
    pro: {
      id: 'pro',
      name: 'Pro',
      description: 'For professionals',
      features: [
        '所有基础功能',
        '1000 积分/月',
        '优先支持',
        '高级功能',
      ],
      prices: [
        {
          type: PaymentTypes.SUBSCRIPTION,
          priceId: process.env.NEXT_PUBLIC_STRIPE_PRICE_PRO_MONTHLY!,
          amount: 990,  // $9.90
          currency: 'USD',
          interval: PlanIntervals.MONTH,
        },
        {
          type: PaymentTypes.SUBSCRIPTION,
          priceId: process.env.NEXT_PUBLIC_STRIPE_PRICE_PRO_YEARLY!,
          amount: 9900,  // $99.00
          currency: 'USD',
          interval: PlanIntervals.YEAR,
        },
      ],
      popular: true,
    },
    // ... 更多计划
  },
};
```

#### 导航配置

编辑 `src/config/navbar-config.tsx`:

```typescript
export const navbarConfig = {
  mainNav: [
    {
      title: 'Features',
      href: '/#features',
    },
    {
      title: 'Pricing',
      href: '/pricing',
    },
    {
      title: 'Blog',
      href: '/blog',
    },
    {
      title: 'Docs',
      href: '/docs',
    },
  ],
};
```

#### 国际化翻译

编辑 `messages/en.json` 和 `messages/zh.json`:

```json
{
  "Metadata": {
    "name": "Your App Name",
    "description": "Your app description",
    "keywords": "saas, nextjs, react"
  },
  "Home": {
    "title": "Welcome to Your App",
    "subtitle": "Build amazing things",
    "cta": "Get Started"
  }
}
```

## 三、模板设计

### 1. 模板架构

#### 核心模块

```
MkSaaS 模板架构:

┌─────────────────────────────────────┐
│         前端层 (Frontend)            │
├─────────────────────────────────────┤
│ - React 19 + Next.js 15             │
│ - Server Components                 │
│ - Client Components                 │
│ - Tailwind CSS + shadcn/ui          │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│        业务逻辑层 (Business)         │
├─────────────────────────────────────┤
│ - Server Actions                    │
│ - API Routes                        │
│ - Middleware                        │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│        数据层 (Data)                 │
├─────────────────────────────────────┤
│ - Drizzle ORM                       │
│ - PostgreSQL                        │
│ - Redis (可选)                      │
└─────────────────────────────────────┘
              ↓
┌─────────────────────────────────────┐
│      第三方服务 (Services)           │
├─────────────────────────────────────┤
│ - Better Auth (认证)                │
│ - Stripe (支付)                     │
│ - Resend (邮件)                     │
│ - S3/R2 (存储)                      │
│ - OpenAI (AI)                       │
└─────────────────────────────────────┘
```

#### 模块化设计

每个功能模块都是独立的，可以单独使用或组合：

```
src/
├── auth/           # 认证模块
├── payment/        # 支付模块
├── credits/        # 积分模块
├── mail/           # 邮件模块
├── storage/        # 存储模块
├── newsletter/     # Newsletter 模块
├── ai/             # AI 模块
└── analytics/      # 分析模块
```

### 2. UI 组件库

#### Blocks 系统

MkSaaS 提供了丰富的 UI Blocks（区块），可以快速组装页面：

```
src/components/blocks/
├── hero/              # Hero 区块
│   └── hero.tsx
├── features/          # 特性区块
│   ├── features.tsx
│   ├── features2.tsx
│   └── features3.tsx
├── pricing/           # 价格区块
│   └── pricing.tsx
├── testimonials/      # 推荐区块
│   └── testimonials.tsx
├── faqs/              # FAQ 区块
│   └── faqs.tsx
├── calltoaction/      # CTA 区块
│   └── calltoaction.tsx
├── logo-cloud/        # Logo 云区块
│   └── logo-cloud.tsx
├── stats/             # 统计区块
│   └── stats.tsx
└── integration/       # 集成区块
    ├── integration.tsx
    └── integration2.tsx
```

#### 使用 Blocks

```typescript
// src/app/[locale]/(marketing)/page.tsx
import { Hero } from '@/components/blocks/hero/hero';
import { Features } from '@/components/blocks/features/features';
import { Pricing } from '@/components/blocks/pricing/pricing';
import { Testimonials } from '@/components/blocks/testimonials/testimonials';
import { CallToAction } from '@/components/blocks/calltoaction/calltoaction';

export default function HomePage() {
  return (
    <>
      <Hero />
      <Features />
      <Pricing />
      <Testimonials />
      <CallToAction />
    </>
  );
}
```

#### shadcn/ui 组件

MkSaaS 集成了完整的 shadcn/ui 组件库：

```
src/components/ui/
├── button.tsx
├── card.tsx
├── dialog.tsx
├── input.tsx
├── select.tsx
├── table.tsx
├── toast.tsx
└── ... 50+ 组件
```

使用示例：

```typescript
import { Button } from '@/components/ui/button';
import { Card, CardHeader, CardTitle, CardContent } from '@/components/ui/card';
import { Input } from '@/components/ui/input';

export function MyComponent() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Title</CardTitle>
      </CardHeader>
      <CardContent>
        <Input placeholder="Enter text..." />
        <Button>Submit</Button>
      </CardContent>
    </Card>
  );
}
```

### 3. 主题系统

#### 多主题支持

MkSaaS 支持多个预设主题：

```typescript
// src/config/website.tsx
ui: {
  theme: {
    defaultTheme: 'default',  // default, blue, green, orange, red, violet
    enableSwitch: true,
  },
}
```

#### 自定义主题

创建新主题：

```css
/* src/styles/themes/my-theme.css */
[data-theme="my-theme"] {
  --background: 0 0% 100%;
  --foreground: 222.2 84% 4.9%;
  --primary: 221.2 83.2% 53.3%;
  --primary-foreground: 210 40% 98%;
  /* ... 更多颜色变量 */
}
```

注册主题：

```typescript
// src/config/website.tsx
ui: {
  theme: {
    defaultTheme: 'my-theme',
    enableSwitch: true,
  },
}
```

### 4. 响应式设计

所有组件都是响应式的，支持移动端、平板和桌面端：

```typescript
// 使用 Tailwind CSS 响应式类
<div className="
  grid 
  grid-cols-1      // 移动端: 1列
  md:grid-cols-2   // 平板: 2列
  lg:grid-cols-3   // 桌面: 3列
  gap-4
">
  {/* 内容 */}
</div>
```

## 四、模板插件与扩展

### 1. 内置插件

MkSaaS 集成了多个插件和库：

#### UI 动画库

```
- Magic UI: 高级动画组件
- Framer Motion: 动画库
- Animate.css: CSS 动画
```

#### 分析工具

```
- Vercel Analytics
- Google Analytics
- PostHog
- Plausible
- Umami
- OpenPanel
```

#### 开发工具

```
- Biome: 代码格式化和 Lint
- TypeScript: 类型检查
- Drizzle Kit: 数据库工具
- React Email: 邮件模板
```

### 2. 添加自定义插件

#### 示例：添加 Sentry

```bash
pnpm add @sentry/nextjs
```

配置：

```typescript
// sentry.client.config.ts
import * as Sentry from '@sentry/nextjs';

Sentry.init({
  dsn: process.env.NEXT_PUBLIC_SENTRY_DSN,
  tracesSampleRate: 1.0,
});
```

#### 示例：添加 Stripe 扩展

```bash
pnpm add @stripe/stripe-js
```

使用：

```typescript
import { loadStripe } from '@stripe/stripe-js';

const stripePromise = loadStripe(
  process.env.NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY!
);
```

### 3. 模板之间的关系

#### 共享核心

所有 5 个模板共享相同的核心架构：

```
共享模块:
├── 认证系统 (Better Auth)
├── 支付系统 (Stripe)
├── 数据库层 (Drizzle ORM)
├── 邮件系统 (Resend)
├── 存储系统 (S3/R2)
└── 基础组件 (shadcn/ui)
```

#### 差异化功能

每个模板有其特定的功能：

```
mksaas-template:
- 通用 SaaS 功能
- 完整的示例代码
- 适合快速启动

mksaas-blog:
- MDX 内容管理
- 博客功能
- SEO 优化
- 适合内容网站

mksaas-haitang:
- 特定领域示例
- 自定义功能
- 适合学习参考

mksaas-outfit:
- 电商功能
- 产品展示
- 适合展示类应用

mksaas-app:
- 纯应用功能
- 最小化内容
- 适合工具类应用
```

#### 代码复用

可以在不同模板之间复制组件和功能：

```bash
# 从 mksaas-blog 复制博客功能到 mksaas-template
cp -r mksaas-blog/content mksaas-template/
cp -r mksaas-blog/src/components/blog mksaas-template/src/components/
cp mksaas-blog/source.config.ts mksaas-template/
```

## 五、获取帮助

### 1. 文档资源

- **官方文档**: [mksaas.com/docs](https://mksaas.com/docs)
- **API 参考**: 查看代码中的 JSDoc 注释
- **示例代码**: 每个模板都包含完整示例

### 2. 社区支持

- **Discord**: [mksaas.link/discord](https://mksaas.link/discord)
- **GitHub Issues**: 提交问题和建议
- **X/Twitter**: [@mksaascom](https://x.com/mksaascom)

### 3. 技术支持

- **邮件**: support@mksaas.com
- **响应时间**: 通常 24-48 小时

### 4. 更新通知

- **Watch 仓库**: 在 GitHub 上点击 Watch 按钮
- **订阅 Newsletter**: 在官网订阅
- **关注社交媒体**: 获取最新动态

## 六、最佳实践

### 1. 开始新项目

1. 选择最适合的模板
2. 克隆到本地
3. 创建自己的 Git 仓库
4. 配置环境变量
5. 自定义配置和样式
6. 开发自己的功能
7. 部署到生产环境

### 2. 保持更新

```bash
# 添加上游仓库
git remote add upstream https://github.com/MkSaaSHQ/mksaas-template

# 获取更新
git fetch upstream

# 合并更新（谨慎操作）
git merge upstream/main

# 或查看差异后手动更新
git diff upstream/main
```

### 3. 自定义建议

- 保持核心架构不变
- 在 `src/` 下创建自己的模块
- 使用配置文件而不是硬编码
- 遵循现有的代码风格
- 编写文档记录自定义功能

## 总结

MkSaaS 模板系统提供：

1. **完整的代码库**: 5 个不同用途的模板
2. **终身访问**: 一次购买，永久使用
3. **持续更新**: 定期更新和新功能
4. **完善文档**: 详细的使用指南
5. **社区支持**: Discord 社区和技术支持
6. **灵活定制**: 可自由修改和扩展
7. **商业许可**: 可用于商业项目

通过 MkSaaS，可以在周末内快速构建一个功能完整的 SaaS 应用。
